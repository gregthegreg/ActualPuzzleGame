if(top = -1)
			{	
			inst  = instance_create_layer(x,y,"Chain",oTopCheck);
			top = inst
			inst.user = self;
				
			}
			
	
	
		if(top =-1)
			{
				
				var inst
			inst  = instance_create_layer(x,y,"Chain",oBottomCheck);
			top = inst
			inst.user = self;
			upmode = true	
				}
		
	